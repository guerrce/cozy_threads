import { Container, Typography } from '@mui/material';
import React, { FC } from 'react';

const OrderCancelledPage: FC<{}> = ({}) => {
  return (
    <Container>
      <Typography variant="h4" >
        Order Cancelled
      </Typography>
    </Container>
  )
};

export default OrderCancelledPage;
