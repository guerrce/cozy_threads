import React, { FC } from 'react';
import NavigationBar from '../../components/NavigationBar/NavigationBar';

const Root: FC<{}> = ({}) => {
  return (
    <div>
      <NavigationBar />
    </div>
  )
};

export default Root;
